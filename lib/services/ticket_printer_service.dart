import 'dart:developer';
import '../models/api_ticket_response.dart';

// 💡 [คำแนะนำ] ควรเปลี่ยนชื่อไฟล์นี้เป็น 'placeholder_ticket_service.dart' หรือลบทิ้ง
// แต่ถ้ายังต้องการให้ Class นี้ดำรงอยู่เพื่อไม่ให้เกิด Error ใน UI:

class TicketPrinterService {
  // ⚠️ ฟังก์ชันนี้ถูกเรียกจาก ReceiptPage.dart
  // แต่เนื่องจากเราพิมพ์เป็นรูปภาพใน ReceiptPage โดยตรงแล้ว
  // ฟังก์ชันนี้จึงไม่มีหน้าที่ทำอะไรอีกนอกจากแจ้งเตือน
  Future<void> printTickets(
    List<ApiTicketResponse> tickets,
    String sellerName,
  ) async {
    log("⚠️ TicketPrinterService.printTickets ถูกเรียก แต่ถูกข้าม.");
    log("▶️ การพิมพ์ตั๋วได้ถูกย้ายไปทำใน ReceiptPage โดยใช้ Image Capture แทน.");
  }
}
